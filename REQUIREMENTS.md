# USB 自动备份与加密工具 —— 需求表

| 项 | 内容 |
|---|---|
| 项目代号 | `usbbackup-r2` |
| 目标平台 | Windows 10 1809+ / Windows 11 / Windows Server 2019+（**amd64，仅 Windows，不跨平台**） |
| 运行形态 | 无 UI：命令行 + 常驻后台进程；可注册为 Windows 服务 |
| 技术栈 | Go 1.24+（开发环境实测 Go 1.27.1），**零第三方依赖，只用标准库** |
| 硬件依赖 | **无 HID 依赖**：不枚举、不监听、不解析任何 HID/输入设备 |
| 文档版本 | 基础需求表 v1.0 · 2026-09-18（R2 上传能力见 [`REQUIREMENTS-R2.md`](REQUIREMENTS-R2.md)） |

---

## 0. 范围说明

### 0.1 范围（相对本需求表早期版本的变更）

| 项 | 早期版本处置 | 现版本处置 |
|---|---|---|
| USB 插入实时监控 | 保留 | **保留**（F-1xx） |
| 私钥文件存在性检测 | 曾以"无差别凭据收集"为由删除 | **恢复纳入**（F-2xx）。约束见 §3 G-01~G-03：**只做存在性布尔判定，绝不读取/复制/落盘/外传私钥内容** |
| 私钥存在 → 本地备份回写 U 盘 | 曾删除 | **恢复纳入**（F-4xx）。约束见 §3 G-04：**只写 `<USB>:\backup\`，不覆盖目录外任何内容，不删除源盘文件** |
| 容量门控 + 整盘压缩 | 保留 | **保留**（F-3xx / F-5xx） |
| 混合加密 / 三件套 CLI | 保留 | **保留**（F-6xx / F-9xx / F-Axx / F-Bxx） |

### 0.2 In Scope
USB 插入事件监控、私钥存在性检测、授权分支（本地备份回写 U 盘 `\backup`）、容量门控、整盘流式 zip、混合加密（AES-256-GCM + RSA-4096-OAEP）、密钥对生成与公钥选择、解密解压、日志与审计、Windows 可执行产物。

### 0.3 Out of Scope（明确不做）
跨平台支持；GUI；HID / 输入设备处理；**读取、复制、导出、上传私钥内容**；删除或改写源盘文件；隐藏进程/窗口/端口；免杀、反调试、绕过安全软件；提权；持久化自启动（服务注册仅由用户显式命令触发）；**采集非白名单介质**。

> **本需求表本身不含网络能力。** 本项目 `usbbackup-r2` 在其之上增加一项**受限出站**——
> 仅向配置的 R2 端点上传本工具自己产出的加密容器，见
> [`REQUIREMENTS-R2.md`](REQUIREMENTS-R2.md) §0.1 与 G-06′。
> 不含任何网络能力的 `UsbBackUP` 是**另一个独立项目**（独立仓库、独立标识、独立配置目录），
> 其 G-06「零网络」仍然有效且可被 `strings` 复核。两者互不依赖，容器格式刻意兼容。

---

## 1. 功能需求表

### 1.1 设备监控模块 `internal/winmon`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-101 | 实时监控 USB 存储设备插入 | P0 | Windows 设备通知 | 盘符到达事件 | 事件通道建立失败 → 自动降级轮询 + WARN | 只监听卷到达；不注册、不解析 HID 设备接口 |
| F-102 | 事件驱动实现：消息专用窗口 `HWND_MESSAGE` + `RegisterDeviceNotificationW`（`DBT_DEVTYP_VOLUME`） | P0 | 设备通知 | 驱动号位掩码 → 新增盘符 | `RegisterClassExW`/`CreateWindowExW` 失败 → 降级轮询 | 窗口不可见、不进任务栏、无输入焦点、不改注册表 |
| F-103 | 轮询兜底：`GetLogicalDrives` 差分（默认 5s，可配） | P1 | 盘符快照 | 新增盘符集合 | 取盘符失败 → 保留上次快照 + WARN | 只读查询，无写入 |
| F-104 | 事件去抖与串行化：同盘符 5s 内重复事件合并；多盘事件入队串行处理 | P1 | 原始事件流 | 作业队列 | 队列满 → 丢弃并 WARN，不阻塞事件线程 | 防事件风暴引发 IO 抖动 |
| F-105 | 启动时枚举已挂载可移动盘（可开关，默认开） | P2 | 盘符列表 | 作业队列 | 单盘失败不影响其余 | — |
| F-106 | 单盘作业超时保护（默认 30 min） | P1 | 作业上下文 | 超时终止 | 超时 → 清理半成品 + ERROR | 防挂起盘堵塞队列 |
| F-107 | 设备身份识别：卷序列号 + 卷 GUID + 盘符 | P0 | 盘符 | 设备指纹 | 任一取值失败 → 退化用盘符 | 指纹仅用于去重与审计，不用于追踪他人 |

### 1.2 私钥存在性检测模块 `internal/keyfile`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-201 | 检测模式 A（启发式）：按文件名模式匹配 | P0 | 目录项名 | 命中列表 | 无命中 → 返回 false | 只比对文件名，不打开文件 |
| F-202 | 检测模式 B（内容特征）：仅读每个候选文件**前 4 KiB** 做魔数/头部特征匹配 | P0 | 文件前 4 KiB | 命中/未命中 | 读取失败 → 跳过 + DEBUG（不中断扫描） | **只读不缓存、不落盘、不打印内容**；读到的字节用完即弃 |
| F-203 | 检测模式 C（显式授权标记）：`<USB>\.usbbackup-r2-allow` 内容为已配置**公钥指纹**时判定为授权 | P0 | 标记文件 | 授权 true/false | 文件缺失 → 走 A/B 判定 | 用**公钥**校验，全程不接触私钥材料 |
| F-204 | 扫描边界：最大深度 4 层、最多 5000 个文件、总耗时上限 20s | P0 | 扫描参数 | 有界结果 | 达上限即停止并 WARN | 有界扫描，防恶意深目录/海量文件拖垮系统 |
| F-205 | 排除系统目录：`System Volume Information`、`$RECYCLE.BIN`、`found.000` 等 | P0 | 目录项 | 跳过项 | — | 避免误判与权限错误 |
| F-206 | 重解析点（符号链接/联接点）不跟随 | P0 | 目录项 | 跳过该项 | — | 防递归循环与越界读取 |
| F-207 | 可配置自定义特征：文件名模式表 + 内容特征表（`config.json`） | P1 | 配置 | 生效规则 | 配置非法 → 用内置默认 | 默认规则表覆盖 PEM/PKCS#8/PKCS#12/JKS/PPK/OpenSSH/PGP/age/WireGuard 等常见形态 |
| F-208 | 判定结果：仅输出布尔值 `authorized` + 命中计数 + 命中类型 | P0 | 扫描结果 | 布尔 + 统计 | — | **审计日志只记 `has_keyfile=true/false` 与计数，不记文件名、不记路径** |

内置默认特征（示例，可扩展）：
- 文件名：`id_rsa` `id_dsa` `id_ecdsa` `id_ed25519` `*.pem` `*.key` `*.ppk` `*.p12` `*.pfx` `*.jks` `*.keystore` `*.p8` `*.pvk` `*.priv` `*.privkey` `*.asc` `*.gpg` `*.skey` `*_key.txt`
- 内容（前 4 KiB）：`-----BEGIN (RSA |EC |DSA |OPENSSH |SSH2 |PGP )?PRIVATE KEY-----`、`-----BEGIN ENCRYPTED PRIVATE KEY-----`、PuTTY `PuTTY-User-Key-File-`、`openssh-key-v1`、`AGE-SECRET-KEY-1`、Windows PVK 头 `B0B0B0B0`、JKS 魔数 `FEEDFEED`、PKCS#12/PFX 头 `3082`、Ethereum keystore JSON 特征 `"crypto"`+`"cipher"`

### 1.3 卷信息与容量门控模块 `internal/winvol`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-301 | 识别可移动卷（`DRIVE_REMOVABLE`），排除固定盘/光驱/网络盘/RAM 盘 | P0 | 盘符 | 候选 true/false | 类型未知 → 视为非候选跳过 | **绝不对系统盘、固定盘执行任何操作** |
| F-302 | 读取总容量 / 空闲容量（`GetDiskFreeSpaceExW` 的 `TotalNumberOfFreeBytes`）→ 计算 `used = total - free` | P0 | 盘符 | total/free/used | 失败重试 3 次（500ms/1s/2s），仍失败 → 保守跳过 | 只读查询 |
| F-303 | 读取卷标 / 文件系统 / 卷序列号（`GetVolumeInformationW`） | P0 | 盘符 | label/fs/serial | 卷标为空 → `VOL_<盘符>` | 序列号仅用于命名与审计 |
| F-304 | 容量门控：`used > threshold`（默认 **10 GiB = 10×1024³**）→ 跳过 | P0 | used | skip/continue | 数值异常（负数、total>0 而 used<0）→ 跳过 + WARN | 默认保守：不确定即跳过 |
| F-305 | 卷就绪等待：刚插入时卷可能未挂载完成 | P0 | 盘符 | 就绪/超时 | 最多等 10s，超时 → 跳过该次 | — |
| F-306 | 剩余空间预检（回写分支用）：U 盘可用空间 ≥ 待写入字节 × 1.05 | P0 | 待写字节 | 通过/拒绝 | 不足 → 中止并 ERROR | 防写满目标盘 |

### 1.4 授权分支：本地备份回写模块 `internal/copier`

> 触发条件：F-2xx 判定 `authorized = true`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-401 | 将本地「备份文件夹」内容复制到 `<USB>:\backup\`，目录不存在则创建 | P0 | 源目录 + 盘符 | 目标目录树 | 创建失败 → ERROR 并放弃该作业 | **仅在 `\backup\` 内写入**；不触碰该目录以外任何对象 |
| F-402 | 增量复制：目标已存在且 `size + mtime` 一致 → 跳过 | P0 | 文件元数据 | 跳过/复制 | 元数据读取失败 → 复制（保守） | 只读源 + 只写目标目录 |
| F-403 | 冲突策略：默认**跳过**已存在文件；`--overwrite` 才覆盖 | P0 | 冲突项 | 跳过/覆盖 | — | 默认不覆盖，防误伤 U 盘既有数据 |
| F-404 | 长路径支持：路径 > 240 字符自动加 `\\?\` 前缀 | P0 | 超长路径 | 可打开路径 | 仍失败 → 跳过 + WARN | — |
| F-405 | 重解析点不跟随；`.lnk`/junction 视为普通文件复制 | P0 | 目录项 | 复制/跳过 | — | 防目录穿越与循环 |
| F-406 | 复制源守卫：若备份文件夹位于该 U 盘内 → 拒绝执行 | P0 | 源/目标路径 | 接受/拒绝 | 命中 → ERROR 放弃作业 | 防自复制套娃 |
| F-407 | 复制后校验（可选，默认开）：逐文件比对大小；`--hash` 时比对 SHA-256 | P1 | 源/目标 | 校验结论 | 不一致 → ERROR + 审计记录 | 只比较哈希值 |
| F-408 | 清单文件 `<USB>:\backup\.usbbackup-r2-manifest.json`（可选）：时间、文件数、字节数、工具版本、**公钥指纹** | P2 | 作业结果 | JSON 文件 | 写失败 → WARN | **清单不含私钥、不含文件内容** |
| F-409 | `--dry-run`：只打印将执行的动作，不做任何写入 | P1 | 参数 | 文本 | — | 首次使用建议先 dry-run |
| F-410 | 回写统计：文件数 / 字节 / 新增 / 跳过 / 失败 | P1 | 过程 | 统计 | — | — |

### 1.5 非授权分支：整盘打包模块 `internal/archive`

> 触发条件：F-2xx 判定 `authorized = false` 且 F-304 容量门控 `continue`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-501 | 流式 zip 打包（`archive/zip`，默认 Deflate） | P0 | 卷根路径 | zip 字节流（管道直送加密器） | 单文件错误 → 跳过 + WARN，不中断整体 | **源盘全程只读**：不创建、不修改、不删除 U 盘任何文件 |
| F-502 | 已压缩格式（jpg/png/mp4/mp3/zip/7z/rar/gz）自动用 Store（不压缩）以省 CPU | P1 | 文件扩展名 | Store/Deflate 选择 | 判断失败 → Deflate | 降低 CPU 与耗时 |
| F-503 | 归档内路径归一化：相对卷根的相对路径 + 正斜杠 | P0 | 绝对路径 | 归档内路径 | 路径越界 → 拒绝该条 + WARN | 防归档内出现绝对路径/盘符/UNC |
| F-504 | 长路径支持（`\\?\` 前缀） | P0 | 超长路径 | 可打开路径 | 仍失败 → 跳过 + WARN | — |
| F-505 | 排除表：`System Volume Information`、`$RECYCLE.BIN`、`pagefile.sys`、`hiberfil.sys`、`swapfile.sys`、`found.*` | P1 | 目录项 | 跳过项 | — | 系统元数据，非用户数据 |
| F-506 | 重解析点不跟随 | P0 | 目录项 | 跳过 | — | 防递归循环、越界读取 |
| F-507 | **源守卫**：输出目录位于扫描源内 → 拒绝执行 | P0 | 源 + 输出路径 | 接受/拒绝 | 命中 → ERROR + 放弃作业 | **防递归套娃膨胀（历史缺陷回归防护）** |
| F-508 | 打包统计：文件数 / 原始字节 / 压缩后字节 / 跳过数 / 耗时 | P1 | 过程 | 统计结构 | — | — |
| F-509 | 树指纹：路径+大小+mtime 滚动 SHA-256 | P2 | 目录元数据 | 32B 指纹 | 计算失败 → 跳过去重，不阻断备份 | 仅哈希元数据，不采集内容 |

### 1.6 混合加密模块 `internal/crypto`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-601 | **混合加密**：随机生成 AES-256 会话密钥加密数据；非对称公钥**只用于包装该会话密钥** | P0 | 明文流 + 公钥 | `.usbk` 容器 | 包装失败 → 中止 + 清理半成品 | 严格满足"非对称仅加密对称密钥" |
| F-602 | 非对称层 v1：RSA-4096 + OAEP(SHA-256)，label `usbbackup/v1`（协议常量，与仓库路径无关） | P0 | 公钥 | 512B 包装密钥 | 密钥长度不符 → 明确报错 | 私钥永不入日志、永不入产物 |
| F-603 | 非对称层 v2（预留）：X25519 ECDH + HKDF-SHA256（标准库 `crypto/ecdh` + `crypto/hkdf`） | P2 | 公钥 | 32B 包装密钥 | 曲线不符 → 报错 | 容器头含算法号，向后兼容演进 |
| F-604 | 对称层：AES-256-GCM 分块流式，默认块 1 MiB | P0 | 明文块 | 密文块 + 16B Tag | Tag 校验失败 → 中止 + 清理 | 每块独立认证 |
| F-605 | 分块 Nonce 构造：`8B 随机前缀 ‖ 4B 块序号(BE)`；每文件/每作业独立随机前缀 | P0 | 块序号 | 12B nonce | 序号溢出 → 报错（不可能达 2³² 块） | **防 nonce 重用** |
| F-606 | 容器头（大端，明文可读）：magic `USBK` / version / 算法号 / 块大小 / nonce 前缀 / 包装密钥 / 明文总长度 / 明文 SHA-256 | P0 | — | 头部字节 | 版本不支持 → 拒绝解析 | 头部不含任何密钥明文 |
| F-607 | 末块标记：末块长度最高位置 1，明确标识流结束 | P0 | 密文流 | 完整性结论 | 缺标记 → 判定截断并报错 | 防静默截断 |
| F-608 | 每块 AAD 绑定：块序号 + 是否末块 + 容器头哈希 | P0 | 块元数据 | AAD | — | 防块重排、跨文件拼接 |
| F-609 | 内存约束 O(块大小)（约 ≤ 16 MiB 峰值），不整份载入 | P0 | — | — | 超限即流式 | 防大文件打爆内存 |
| F-610 | 解密：私钥解包会话密钥 → 逐块解密 + 认证 + 尾部标记校验 | P0 | `.usbk` + 私钥 | 明文流 | 失败 → 统一错误"解密或完整性校验失败" | **错误信息不区分"密钥错"与"数据被篡改"，避免 oracle 泄露** |
| F-611 | 明文完整性核对：全量 SHA-256 与头部记录值比对 | P1 | 明文流 | 一致/不一致 | 不一致 → 报错 | — |

### 1.7 密钥管理模块 `internal/keystore`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-701 | 生成密钥对：RSA 默认 4096（`--bits` 下限 2048） | P0 | 参数 | `usbbackup-r2.key.pem`（PKCS#8）+ `usbbackup-r2.pub.pem`（PKIX） | 目标存在且无 `--force` → 拒绝覆盖 | 生成后立即收紧 ACL：仅当前用户可读 |
| F-702 | 私钥口令保护（可选 `--pass`）：PBKDF2-HMAC-SHA256（标准库 `crypto/pbkdf2`，60 万次）→ AES-256-GCM 封装 PKCS#8 → `ENCRYPTED PRIVATE KEY` | P1 | 口令 | 加密私钥 PEM | 口令空/过短（<8）→ 拒绝 | 私钥不加密落盘时给出显著 WARN |
| F-703 | 选择已有公钥（`use`）：校验为公钥后写入 `config.json` | P0 | 公钥 PEM 路径 | 配置更新 | 误传私钥 → 检测并拒绝；非 PEM → 拒绝 | **只接受公钥；私钥不进配置** |
| F-704 | 公钥指纹（SHA-256，分组长十六进制）+ `inspect` 展示 | P1 | 公钥 | 指纹字符串 | 解析失败 → 报错 | 指纹可安全外显；私钥内容永不外显 |
| F-705 | `selftest`：临时内存数据加解密往返 + 篡改检测验证 | P1 | 无 | 通过/失败 | 失败 → 非零退出码 | 自检数据不落盘 |
| F-706 | 私钥口令交互读取：禁用回显（`golang.org/x/term` 不用，自实现 `SetConsoleMode` 关闭 ECHO） | P1 | 键盘 | 口令字节 | 非终端环境 → 要求 `--pass-file` | 支持从文件读口令，避免命令行明文留痕 |

### 1.8 流水线编排模块 `internal/backup`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-801 | 作业流水线：插入事件 → 就绪等待 → 卷类型 → **私钥检测** → 分支 A(回写) / 分支 B(容量门控 → 打包加密) | P0 | 盘符 | 作业结果 | 任一步失败 → 清理半成品 + 标记失败，不影响后续作业 | 源盘只读（分支 A 仅写 `\backup\`） |
| F-802 | 产物命名（分支 B）：`{卷标}.zip.usbk`；卷标净化（`\ / : * ? " < > \|` → `_`，空 → `VOL_<盘符>`） | P0 | 卷标 | 文件名 | 重名 → 追加 `_YYYYMMDD_HHMMSS` | **防路径穿越**；净化后不含任何路径分隔符 |
| F-803 | 产物落盘位置：`%TEMP%\backup\`（可配置） | P0 | 加密流 | 文件 | 磁盘满 → 中止 + 清理半成品 | 明文 zip 默认不落盘（见 D-03） |
| F-804 | 作业超时与可中断：Ctrl+C / 服务停止 → 安全中止当前作业 | P1 | 信号 | 退出码 | 强制终止 → 半成品由下次启动清理 | 绝不因中断而损伤源盘数据 |
| F-805 | 内容去重（默认开）：同卷树指纹一致 → 跳过并复用上次产物 | P1 | 树指纹 | 跳过/执行 | 指纹缺失 → 执行 | 去重索引只存指纹与元数据 |
| F-806 | 保留策略：同卷最近 N 份（默认 5），超出删最旧 | P2 | 输出目录 | 删除计数 | 删除失败 → WARN，不阻断 | **只删本工具自己生成的 `.usbk` 产物**；不删源盘、不删其他文件 |
| F-807 | 审计日志 `audit.jsonl`：时间/盘符/卷标/序列号/容量/**授权分支**/文件数/指纹/产物路径/耗时/结果 | P0 | 作业结果 | JSON Lines | 写失败 → WARN | **不含私钥、不含文件名清单、不含内容** |
| F-808 | 单实例互斥：命名 Mutex，重复启动直接退出 | P1 | 启动 | 退出码非零 | — | 防重复挂载监控 |
| F-809 | 半成品清理：启动时扫描 `.part` 残留并清理 | P2 | 输出目录 | 删除计数 | 失败 → WARN | 只删本工具命名的临时文件 |

### 1.9 生成器 CLI `cmd/usbkeygen-r2`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-901 | **启动时显著展示「安全警告」+「免责声明」**（醒目横幅，非滚动淹没） | P0 | 无 | 控制台输出 | — | 必须含：仅限自有机器与自有介质、介质白名单默认拒绝、不删源文件、不采集私钥内容、唯一出站为上传到自配 R2 端点、无隐蔽能力、凭据可被本机管理员提取的残余风险 |
| F-902 | 强制确认：须输入 `I AGREE` 才继续（`--yes` 供自动化跳过） | P0 | 用户输入 | 继续/退出 | 不匹配 → 退出码 2 | 防无意识使用 |
| F-903 | `generate` 生成密钥对（`--bits` / `--pass` / `--force` / `--out`） | P0 | 参数 | PEM 文件 | 位低于 2048 → 拒绝 | 见 F-701/F-702 |
| F-904 | `use` 选择已有公钥写入配置 | P0 | 公钥路径 | `config.json` | 误传私钥 → 拒绝并提示 | 见 F-703 |
| F-905 | `inspect` 查看公钥指纹；`selftest` 自检 | P1 | 参数 | 输出 | 失败 → 非零退出 | 不回显私钥任何字节 |
| F-906 | 免责声明采用 **CC BY-NC-SA 4.0**（正文 `DISCLAIMER.md`，生成器内置摘要 + 指向链接） | P0 | 无 | 展示文本 | — | — |

### 1.10 压缩器 CLI `cmd/usbcomp-r2`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-A01 | `pack <目录> -o <输出.usbk>`：目录 → 流式 zip → 混合加密 → 单文件容器 | P0 | 目录 + 公钥 | `.usbk` | 公钥缺失 → 明确提示先 `usbkeygen-r2 use` | 只读源目录 |
| F-A02 | 复用 `internal/archive` 全部守卫（路径归一化、长路径、排除表、源守卫、不跟随重解析点） | P0 | 同上 | 同上 | 同上 | 同 F-503~F-507 |
| F-A03 | `--store` 强制不压缩 / `--level` 压缩级别；`--threads` 并发压缩（默认 CPU-1，上限 8） | P2 | 参数 | 产物 | 参数非法 → 报错 | 资源上限受控 |
| F-A04 | `--keep-plain-zip`：额外保留明文 zip（默认关闭，开启时显著 WARN） | P2 | 参数 | `.zip` | — | 明文产物为敏感数据，需用户自行处置 |
| F-A05 | 进度输出：文件数/字节/速率（`--quiet` 静默） | P1 | 过程 | 控制台 | — | — |

### 1.11 解压器 CLI `cmd/usbunseal-r2`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-B01 | `list`：显示容器头元信息（版本/算法/块大小/明文长度/公钥指纹） | P1 | `.usbk` | 文本 | 头部损坏 → 报错 | 不打印任何密钥材料 |
| F-B02 | `verify`：解密并校验完整性 + 明文 SHA-256，不落明文 | P1 | `.usbk` + 私钥 | 通过/失败 | 任一失败 → 非零退出 | — |
| F-B03 | `unseal`：解密 + 解压到显式指定目录（`-d <dir>` 必填） | P0 | `.usbk` + 私钥 | 目录树 | 磁盘满/权限 → 中止 + 清理 | 输出目录必须显式指定，防误写 |
| F-B04 | `--keep-zip`：只解出 zip 不解压 | P2 | 同上 | `.zip` | — | 提示用户操作后妥善处置明文 zip |
| F-B05 | **防 Zip Slip**：拒绝 `..`、绝对路径、盘符、UNC、符号链接/重解析点条目 | P0 | 归档条目名 | 接受/拒绝 | 命中 → 拒绝该条 + ERROR | 防解压穿越覆盖系统文件 |
| F-B06 | 解压冲突检查：目标已存在 → 默认跳过；`--force` 覆盖 | P1 | 目标路径 | 跳过/覆盖 | — | 默认不覆盖，防误伤 |
| F-B07 | 解压前后磁盘空间预检（声明解压后大小 ≥ 可用空间 → 提前拒绝） | P1 | 元数据 | 通过/拒绝 | 拒绝 → 非零退出 | 防写满磁盘 |

### 1.12 日志与配置

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-C01 | 分级日志 DEBUG/INFO/WARN/ERROR（`log/slog`），级别可配置（`--log-level` / 环境变量） | P0 | 级别 | 控制台 + 文件 | 文件不可写 → 仅控制台 + WARN | 日志永不打印私钥/会话密钥/口令 |
| F-C02 | 日志轮转：按大小（默认 10 MiB × 5 份），标准库自实现 | P1 | 阈值 | 轮转文件 | 轮转失败 → 追加写不中断 | — |
| F-C03 | 配置 `config.json`：阈值、备份源目录、输出目录、公钥路径、检测模式与特征表、去重开关、保留份数、日志、超时、轮询间隔 | P0 | 文件 | 运行参数 | 缺失 → 内置默认；格式错误 → 明确报错退出 | **配置不含私钥** |
| F-C04 | 覆盖优先级：命令行 flag > 环境变量（`USBBACKUP_R2_*`）> `config.json` > 内置默认 | P1 | 多层 | 生效配置 | 冲突 → 高优先级生效 + DEBUG 记录来源 | — |
| F-C05 | 配置自检 `config-check`：校验源目录存在、公钥可解析、输出目录可写 | P1 | 配置 | 检查报告 | 任一失败 → 非零退出 | — |

### 1.13 服务化与部署

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-D01 | 子命令 `run`（前台控制台）/ `once --drive X:`（单次执行，用于验证） | P0 | 参数 | 运行 | 参数非法 → 用法提示 | — |
| F-D02 | 服务注册：`install-service` / `uninstall-service` / `start` / `stop`，基于 `sc` 语义（`golang.org/x/sys/windows/svc` 不用，改用 `sc.exe` 调用或纯 stdlib 服务控制） | P2 | 参数 | 服务状态 | 需管理员权限 → 明确提示 | **仅用户显式命令触发**，不静默自启动 |
| F-D03 | 构建产物：`usbbackup-r2.exe` / `usbkeygen-r2.exe` / `usbcomp-r2.exe` / `usbunseal-r2.exe`（`-H windowsgui` 仅用于后台主程序） | P0 | 构建 | exe | — | 版本信息内置，`version` 可查 |
| F-D04 | 构建脚本 `build.ps1` / `build.sh`：注入版本号与构建时间（`-ldflags`），可选 UPX（**默认不启用**） | P1 | 版本 | exe | 构建失败 → 非零退出 | 不做任何加壳/免杀处理 |

---

## 2. 非功能需求

| 编号 | 类别 | 要求 | 验收方式 |
|---|---|---|---|
| N-101 | 性能 | 空闲态 CPU ≈ 0（事件驱动）；打包受源盘 IO 上限约束；峰值内存 ≤ 约 16 MiB | 任务管理器采样 + 大文件测试观察驻留内存 |
| N-102 | 资源 | 单实例互斥；重复启动立即退出 | 连开两次，第二次非零退出并提示 |
| N-103 | 兼容 | Windows 10 1809+ / 11 / Server 2019+，amd64 | 目标机实跑 `once --drive` |
| N-104 | 依赖 | **仅 Go 标准库，零第三方依赖**，可离线构建 | `go.mod` 无 `require`；断网 `go build ./...` 通过 |
| N-105 | 可维护 | 模块化：监控/检测/容量/复制/压缩/加密/密钥/编排 独立包，依赖单向 | 包依赖图无环 |
| N-106 | 可测试 | 核心路径有单测：容量门控、卷标净化、路径守卫、加解密往返、篡改检测、Zip Slip、私钥特征匹配 | `go test ./...` 全绿 |
| N-107 | 交付 | 4 个 exe 产物 | 产物存在且 `version` 可执行 |
| N-108 | 版本管理 | Git 仓库 + Conventional Commits，提交可按编号回滚 | `git log --oneline` 规范 |
| N-109 | 稳定性 | 任何单盘失败不影响其他盘；任何异常不导致进程崩溃（顶层 recover + 日志） | 拔盘中断测试 |

---

## 3. 安全边界与合规约束（硬性）

| 编号 | 约束 |
|---|---|
| G-01 | **私钥检测只做存在性布尔判定**：仅比对文件名模式与前 4 KiB 文件头特征。不读取、不解析、不复制、不缓存、不落盘、不外传任何私钥字节；读入的 4 KiB 缓冲区用后立即清零 |
| G-02 | **审计与日志不记录私钥文件名/路径/内容**，只记录 `has_keyfile=true/false`、命中计数与命中类型（如 `pem-header`） |
| G-03 | 检测结果仅在本地内存中作为分支条件使用，不产生任何副产品文件 |
| G-04 | **回写只写 `<USB>:\backup\`**：不覆盖该目录外任何对象、不删除 U 盘任何文件、不修改 U 盘任何既有文件的属性/时间戳 |
| G-05 | **源盘只读**：非授权分支对 U 盘全程只读；两个分支都绝不删除、移动、改名、改写源盘文件 |
| G-06 | ~~**零网络**：全部二进制不含任何 socket/HTTP/DNS 调用，不具备上传能力~~ **已修订为 G-06′**（原文保留作历史记录） |
| **G-06′** | **单向最小出站**：仅允许向配置的 R2 endpoint 主机的 HTTPS 出站，用途仅限上传本工具产出的加密容器。**不得引入任何入站监听、不接受远程指令、不存在控制通道（C2）、不做遥测上报内容**；除该 endpoint 外不得有其它出站调用（代码层强制校验目标主机）。仍可用 `strings` 复核出唯一目标主机 |
| G-12 | **介质准入**：仅处理卷序列号白名单内的可移动介质；**白名单为空时拒绝一切采集作业**；未准入介质零读取（连文件名都不枚举），只记审计告警 |
| G-13 | **凭据不进二进制**：R2 凭据以 DPAPI 加密文件在部署时下发，权限最小化、可独立轮换；**不接受 Secret Access Key 编译进 exe** |
| G-14 | **出站面收敛**：唯一允许的出站目标是配置的 R2 endpoint；仅 HTTPS 且强制证书校验，不提供跳过校验的开关 |
| G-15 | **使用边界收紧**：客户端仅部署于本人自有或本人管理的 Windows 机器；仅处理本人自有的可移动介质。**该前提不成立时本项目不适用** |
| G-07 | **无隐蔽能力**：不隐藏进程/窗口/端口/文件，不写自启动项，不修改注册表（服务注册仅由用户显式命令触发），不做加壳/免杀 |
| G-08 | **源守卫**：输出目录若位于扫描源之内（或反之）→ 拒绝执行，防递归膨胀 |
| G-09 | **防路径穿越**：产物命名净化；压缩端产出相对路径；解压端拒绝 `..`/绝对路径/盘符/UNC/符号链接条目 |
| G-10 | **使用边界**：仅限本人自有或已获得明确书面授权的设备与存储介质。不得用于未授权获取他人数据 |
| G-11 | 免责声明与许可：免责声明完整随附（`DISCLAIMER.md`），许可采用 **CC BY-NC-SA 4.0** |

---

## 4. 关键设计决策

| 编号 | 决策 | 理由 | 备选与否决原因 |
|---|---|---|---|
| D-01 | 事件监控用**隐藏的顶层窗口**接收 `WM_DEVICECHANGE`（`WS_POPUP`，不设 `WS_VISIBLE`），轮询兜底 | 卷到达/移除事件由系统**默认广播**给所有顶层窗口，一个隐藏顶层窗口即可收到，空闲态零轮询 | **原方案已否决**：改用消息专用窗口 `HWND_MESSAGE` + `RegisterDeviceNotificationW` 注册卷过滤器。实机验证暴露两处错误——① 消息专用窗口**不在广播范围内**，收不到 `WM_DEVICECHANGE`；② `DBT_DEVTYP_VOLUME` 根本不是 `RegisterDeviceNotification` 支持的过滤器类型（只支持 DEVICEINTERFACE / HANDLE 等），调用会直接返回 `ERROR_INVALID_DATA(13)`。纯轮询：不"实时"；WMI 事件订阅：需 COM，复杂易挂 |
| D-02 | 容量阈值默认按 **GiB**（10×1024³） | 与 Windows 原生显示口径一致 | 十进制 GB 差 7.4%，会放行/拦截边界盘 |
| D-03 | 分支 B **明文 zip 默认不落盘**，流式直送加密器，最终产物 `{卷标}.zip.usbk` | 需求要求"zip 必须混合加密"，留明文 zip 等于加密失效；同时省一次全量磁盘写。**可通过 `--keep-plain-zip` 显式恢复旧行为** | 先落 zip 再加密：残留明文 + 双倍 IO。（待用户确认，见 Q-02） |
| D-04 | 非对称层默认 RSA-4096-OAEP(SHA-256) | 标准库原生、零依赖、PEM 互操作好；每次备份非对称操作仅 1 次，性能无关 | X25519+HKDF 更现代但密钥分发习惯不同；容器头已留算法号（F-603 可后加） |
| D-05 | 对称层 AES-256-GCM 分块（STREAM 式）+ AAD 绑定块序号 | GCM 单密钥有 64 GiB 上限且需认证；分块后流式、可认证、内存恒定 | 整份 GCM：需全量入内存；AES-CTR+HMAC：需额外管理 MAC 密钥 |
| D-06 | 输出到 `%TEMP%\backup\` | 需求指定；`%TEMP%` 常可写（嵌入式 Windows 常启用 UWF，C 盘可能只读或重启还原） | 写其他目录：不符合需求 |
| D-07 | 私钥检测"存在性"用**文件名特征 + 前 4 KiB 头特征 + 可选显式授权标记（公钥指纹）**三路 | 需求要求"不限制具体格式"，启发式覆盖最广；显式标记提供确定性且**不需要私钥材料** | 解析私钥做配对校验：需要接触私钥内容，违反 G-01，否决 |
| D-08 | 树指纹去重 + 同卷保留最近 N 份 | 防反复插拔同一盘造成无限累积 | 无去重：产物无限增长 |
| D-09 | 口令 KDF 用标准库 `crypto/pbkdf2`（60 万次），不用 Argon2id | 保住 N-104 零依赖硬约束 | Argon2id 抗 GPU 更强，但需 `x/crypto`，破坏零依赖；记为后续可选项 |
| D-10 | 服务注册仅由显式命令触发，不做静默自启动 | 符合 G-07 无隐蔽能力 | 静默自启触碰持久化红线，否决 |
| D-11 | 服务模式下**跳过 `I AGREE` 交互确认**（静默进入） | SCM 启动的进程没有控制台，读确认会得到 EOF 并立刻退出，SCM 侧表现为 1053「服务没有及时响应启动或控制请求」。用户显式执行 `install-service` 即为授权行为 | 在服务里打印横幅：无控制台，无意义；要求配置 `--yes`：与 `run` 直接启动不一致，易踩坑 |
| D-12 | 服务（LocalSystem）与交互运行时使用**相同的路径模板**，但解析结果不同 | 这是 Windows 的既有行为，不是缺陷：LocalSystem 下 `%LOCALAPPDATA%` = `C:\Windows\System32\config\systemprofile\AppData\Local`。因此服务部署**必须**在 `config.json` 中写绝对路径（或改用 `%ProgramData%`） | 自动改写路径：会让配置文件与运行结果不一致，反而更难排查 |

---

## 5. 待确认项（需你拍板）

| 编号 | 待确认 | 当前默认 | 影响 |
|---|---|---|---|
| Q-01 | "10GB" 是 **GiB** 还是十进制 GB | **两者都支持**：配置同时接受 `used_threshold`（`10GiB`/`10GB`/`1.5TiB`）与 `used_threshold_bytes`，默认 GiB；两种写法冲突时校验报错 | 边界盘（约 10.0–10.7 GB 占用）放行/拦截行为 |
| Q-02 | 分支 B 是否保留明文 zip | **不保留**，只留 `.usbk` | 忘私钥即不可恢复；保留则削弱加密意义 |
| Q-03 | 备份源目录（回写分支的本地「备份文件夹」）默认路径 | `%USERPROFILE%\usbbackup-r2\local-backup` | 分支 A 的实际数据来源 |
| Q-04 | 是否需要 Windows 服务化（F-D02） | 提供，但仅手动触发 | 开机自启方式与权限模型 |
| Q-05 | 同卷保留份数 N | 5 | 磁盘占用 |
| Q-06 | SD 读卡器等其它 `DRIVE_REMOVABLE` 介质是否同样处理 | 是 | 处理范围 |
| Q-07 | 非授权分支整盘打包，是否设置体积上限 | **加，默认 10 GiB**（`max_total_bytes`，`0` 表示不限制）；比较对象是**待打包数据量**（已占用容量）而非介质容量 | 极端容量盘的耗时与磁盘占用 |
| Q-08 | 是否需要 32 位（386）产物 | 不提供，仅 amd64 | 老设备兼容性 |

---

## 5.1 部署形态：生成器 → 客户端 → 解压器

这是本项目**首选的部署形态**：生成器在你的机器上跑一次，产出的客户端自带配置与公钥。

| 角色 | 程序 | 运行位置 | 持有内容 |
|---|---|---|---|
| 生成器 | `usbkeygen-r2 build-client` | 你的机器 | 生成密钥对，**私钥留在本地** |
| 客户端 | 产出的 `client.exe` | 目标机器 | 仅**公钥 + 配置**，硬编码进 exe |
| 解压器 | `usbunseal-r2` | 你的机器 | 用**私钥**解密还原 |

### 功能点

| 编号 | 需求 | 优先级 | 实现要点 |
|---|---|---|---|
| F-K01 | 生成器可产出内嵌配置与公钥的客户端 exe | P0 | 复制客户端模板 + 尾部追加配置块；不要求目标机器有 Go 工具链 |
| F-K02 | 客户端启动时从**自身可执行文件**读取内嵌配置 | P0 | PE 尾部 overlay + SHA-256 校验；损坏则报错并退回常规路径（不静默用可疑配置） |
| F-K03 | 客户端模式下**忽略**外部 config.json 与环境变量 | P0 | 否则"硬编码"可通过改配置绕过；`--config` 被明确忽略并提示 |
| F-K04 | 内嵌内容**只能是公钥** | P0 | `embedcfg.EnsureNoSecret` 拒绝一切私钥特征；客户端会落在他人机器上，内嵌即公开 |
| F-K05 | 客户端不得自我隐藏、不得自启动 | P0 | 遵循 G-07：无加壳、无自启动项、服务仅由显式命令注册 |
| F-K06 | 客户端仍保留 `probe` 等只读诊断与免责声明 | P1 | 免配置不等于免知情 |
| F-K07 | 首次使用以**命令**确认（`accept`），之后 `run`/`once` 静默运行 | P0 | 确认一次写入本机明文记录；未确认时 `run`/`once` 拒绝并提示先 `accept` |
| F-K08 | 确认记录可查看、可撤销 | P1 | `accept --check` / `--revoke`；伪造或缺确认短语的记录按未确认处理 |
| F-K09 | `--yes` 只跳过当次确认，**不写记录** | P0 | 避免"跑一次脚本就永久静默"；永久静默必须显式 `accept` |

### 为什么用尾部追加而不是重新编译

重新编译要求目标机器有 Go 工具链，不现实；`//go:embed` 把客户端模板嵌进生成器
会让生成器体积翻倍且与客户端版本强耦合。PE 文件尾部追加数据不影响加载运行
（自解压安装包的成熟做法），生成器只需复制一份 exe 再追加一个带校验和的块即可。

### 安全边界

- 客户端**没有私钥**：即使 exe 被完整拷走，也解不开它自己产出的 `.usbk`。
- 任何把私钥塞进客户端的尝试在生成阶段即被拒绝（误传 `key.pem` 会直接报错退出）。
- 客户端不具备解密能力，不存在"客户端自己解开备份"的路径。

---

## 6. 需求 → 模块映射

| 需求段 | 模块/产物 | 主要文件 |
|---|---|---|
| 1.1 监控 | `internal/winmon` | `winmon_windows.go`, `poller.go`, `debounce.go` |
| 1.2 私钥检测 | `internal/keyfile` | `keyfile.go`, `patterns.go` |
| 1.3 容量门控 | `internal/winvol` | `winvol_windows.go`, `gate.go` |
| 1.4 回写 | `internal/copier` | `copier.go`, `walk.go` |
| 1.5 打包 | `internal/archive` | `zipwriter.go`, `walker.go`, `safejoin.go` |
| 1.6 加密 | `internal/crypto` | `container.go`, `encrypt.go`, `decrypt.go`, `nonce.go` |
| 1.7 密钥 | `internal/keystore` | `keygen.go`, `pubkey.go`, `fingerprint.go`, `passphrase.go` |
| 1.8 编排 | `internal/backup` | `pipeline.go`, `retention.go`, `audit.go`, `dedupe.go` |
| 1.9 生成器 | `cmd/usbkeygen-r2` | `main.go`, `banner.go` |
| 1.10 压缩器 | `cmd/usbcomp-r2` | `main.go` |
| 1.11 解压器 | `cmd/usbunseal-r2` | `main.go`, `unzip.go` |
| 1.12 日志配置 | `internal/logx`, `internal/config` | `logx.go`, `rotate.go`, `config.go` |
| 1.13 服务化 | `cmd/usbbackup-r2` | `main.go`, `service_windows.go` |
| 公共 | `internal/fsutil` | `fsutil.go`, `longpath.go`, `reparse.go` |

---

## 7. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 需求表 + 开发环境 + 骨架 + 文档三件套 + Git | ✅ 完成 |
| M1 | `internal/crypto` + `internal/keystore` + 单测（最高风险件先做） | ✅ 完成（含单测） |
| M2 | `internal/archive` 打包/解包执行器 + `internal/copier` 复制执行器 + 单测 | ✅ 完成（含单测） |
| M3 | `internal/winvol` + `internal/keyfile` + 单测 | ✅ 完成（含单测） |
| M4 | `internal/winmon` + `internal/backup` 流水线 + `internal/winsvc` 服务化 | ✅ 完成（含单测） |
| M5 | 4 个 CLI + Windows 实机验证 + 构建脚本 | ✅ 完成 |
| M6 | `internal/cred`（DPAPI）+ `internal/r2`（SigV4 + 分片 + 重试）+ 单测 | ⬜ 待开始 |
| M7 | `internal/mediaauth` 准入 + 客户端流程改造（F-H01~F-H04）+ 生成器扩展（F-907~F-910） | ⬜ 待开始 |
| M8 | Windows 实机验证（真实 R2 桶）+ 文档更新 + 规范提交 | ⬜ 待开始 |

### 7.1 实机验证记录（2026-09-13）

全部通过真实二进制执行，非仅单测：

| 场景 | 结果 |
|---|---|
| 容量门控：已占用 193 GiB > 阈值 10 GiB | 正确跳过，审计记录 `branch=skipped`、`skip_reason=used-over-threshold` |
| 分支 B：提高阈值后整盘打包加密 | 生成 `VOL_X.zip.usbk`；`unseal` 解出后与原始目录 **逐字节一致** |
| 分支 A：介质含 `id_rsa` | 识别为已授权，回写 3 个文件到 `\backup\`，源目录零改动，清单已生成 |
| 常驻 `run`（事件通道 + 模拟到达） | 两个分支均正确执行，作业队列串行化生效 |
| 单实例互斥 | 第二个实例被拒，退出码 4 |
| Zip Slip | 真实构造含 `../`、绝对路径条目的容器 → 2 个条目被阻断，无文件逃出目标目录，退出码 5 |
| 错误私钥 / 篡改 / 截断 | 全部被拒绝 |
| Windows 服务 | install → start（STATE 4 RUNNING，进程在 Services 会话）→ stop → uninstall 全链路通过，卸载后无残留 |

### 7.2 由"跑一遍"发现并修复的缺陷（单测无法覆盖）

| # | 缺陷 | 后果 |
|---|---|---|
| 1 | `fsutil.IsSubPath` 对卷根重复追加分隔符（`E:\` → `E:\\`） | 所有子路径判定恒为假，源守卫与自复制守卫**双双失效** |
| 2 | `winvol.Query` 对非可移动卷提前返回 | 容量/就绪全为 0，诊断输出无用 |
| 3 | `flag` 包遇位置参数即停止解析 | `usbkeygen-r2 use <路径> --config x`、`usbcomp-r2 pack <目录> -o out` 全部失效 |
| 4 | 容器头把算法方案标为固定 `RSA-4096` | 与实际 2048 位密钥不符，误导运维 |
| 5 | `DEV_BROADCAST_VOLUME.dbcv_size` 用 `unsafe.Sizeof` 填充（Go 结构体对齐补到 20，C 结构为 18） | `RegisterDeviceNotificationW` 返回 `ERROR_INVALID_DATA(13)`，事件通道完全不可用 |
| 6 | 服务模式仍要求 `I AGREE` 交互确认 | SCM 拉起进程立即退出，报 1053「服务没有及时响应启动或控制请求」 |
| 7 | 直接把 `sc.exe` 输出转述给用户 | 中文系统下 sc.exe 输出 GBK，按 UTF-8 解码成乱码；改为翻译退出码 + 只取 ASCII 行 |

