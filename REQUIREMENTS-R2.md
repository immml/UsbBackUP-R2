# usbbackup-r2 —— R2 加密上传能力需求表

| 项 | 内容 |
|---|---|
| 项目代号 | `usbbackup-r2` |
| 基础文档 | [`REQUIREMENTS.md`](REQUIREMENTS.md)（本文档只写 R2 上传相关能力，不重复基础段落） |
| 目标平台 | Windows 10 1809+ / Windows 11 / Windows Server 2019+（**amd64，仅 Windows**） |
| 运行形态 | 无 UI：命令行 + 常驻后台进程；可注册为 Windows 服务 |
| 技术栈 | Go 1.24+（开发环境实测 Go 1.27.1），**零第三方依赖，只用标准库** |
| 文档版本 | v1.0 · 2026-09-18 |
| 授权前提 | **客户端仅部署于本人自有或本人管理的机器；仅处理本人自有的可移动介质**（见 §5 G-15） |

---

## 0. 范围说明

### 0.1 与「不含出站能力版本」的差异

| 项 | 不含出站能力版本 | 本项目（含 R2 上传） |
|---|---|---|
| 网络能力 | **零网络**（G-06），全部二进制不含 socket/HTTP/DNS | **修订为单向最小出站**（G-06′ / G-14）：仅 HTTPS 到配置的 R2 endpoint，用于上传加密产物；**无入站监听、无远程指令、无 C2 通道** |
| 非授权分支产物去向 | 仅落盘 `%TEMP%\backup\` | 加密后**上传 Cloudflare R2**；本地是否保留由配置决定（F-E12） |
| 客户端凭据 | 仅内嵌公钥（`embedcfg`） | 新增 **R2 受限 token**，以 DPAPI 加密文件在部署时下发，**不编译进 exe**（§3） |
| 采集策略 | 无 | 新增 `collect_policy` 三档显式策略（G-12 / F-G01）。**曾计划用卷序列号白名单，因序列号在格式化后即改变而废弃**（D-20） |
| 使用边界 | 自有或已授权设备 | **收紧**：自有机器 + 自有介质（G-15） |
| 依赖约束 | 零第三方依赖（N-104） | **不变**。S3 SigV4 与 R2 交互以标准库手写实现（D-13） |

### 0.2 In Scope

在基础需求表全部能力之上，新增：S3 SigV4 签名、R2 分片上传与断点续传、上传进度日志与重试、上传后校验、凭据的 DPAPI 保护与轮换、采集策略三档（`collect_policy`）、客户端分支 B 流程改造、生成器的 R2 参数配置与 `r2-check` 探活。

### 0.3 Out of Scope（明确不做）

- **采集非自有介质**：这是使用边界（G-15），由使用者自行保证；工具不再尝试用硬件身份自动判定（D-20）。`collect_policy=marker_only` 是可选的加固手段，不是权属证明。
- **把 Secret Access Key 编译进 exe**：不接受此方案，理由见 §3（F-F08）。
- 加壳、免杀、反调试、绕过安全软件、隐藏进程/窗口/端口。
- 提权；持久化自启动；任何形式的远程指令通道。
- 读取、复制、导出、上传私钥内容（沿用 G-01~G-03）。
- 远端对象生命周期管理（建议在 R2 bucket 侧配 lifecycle rule，工具不负责远端删除）。

---

## 1. 功能需求表

### 1.1 R2 上传模块 `internal/r2`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-E01 | S3 SigV4 签名（标准库 `crypto/hmac` + `crypto/sha256` 手写） | P0 | 凭据 + 规范化请求 | `Authorization` 头 | 本机时钟偏移 → 服务端 403，提示校时 | 签名密钥（`SigningKey`）仅内存，用后清零 |
| F-E02 | 单次 PUT 上传（对象 ≤ 5 GiB） | P0 | `.usbk` 文件 | 远端对象 | 失败 → 按 F-E05 重试 | 只上传加密产物，不上传明文 |
| F-E03 | 分片上传：`CreateMultipartUpload` / `UploadPart` / `CompleteMultipartUpload`；阈值 5 GiB，分片默认 64 MiB（S3 下限 5 MiB） | P0 | 大对象 | 远端对象 | 分片失败 → 重试该片；超次数 → `AbortMultipartUpload` 清理 | 失败必须 Abort，防远端残留分片计费 |
| F-E04 | 断点续传：本地持久化 `uploadID` + 各分片 ETag，重启后 resume | P1 | 续传状态文件 | 续传/重传 | 状态损坏或 `uploadID` 失效 → 从头开始 | 状态文件不含凭据 |
| F-E05 | 指数退避重试：默认 5 次（1s/2s/4s/8s/16s + 抖动）；`403`/`404`/`400` 不重试，直接失败并明确报错 | P0 | 失败响应 | 重试/放弃 | 重试耗尽 → 保留产物 + 审计 `result=failed` | 不重试语义错误，避免无效重放 |
| F-E06 | 进度日志：分片序号 / 已传字节 / 总字节 / 瞬时与平均速率 / 预估剩余时间 | P0 | 上传过程 | 日志行 | 日志写失败 → 降级控制台 | 只记字节数与序号，**不记 URL query、不记 token** |
| F-E07 | 上传后校验：`HEAD` 比对远端对象大小；明文 SHA-256 写入对象元数据（`x-amz-meta-sha256`）供事后核对 | P0 | 上传结果 | 校验结论 | 不一致 → 标记失败 + 重传一次 | 元数据只含哈希，不含内容 |
| F-E08 | 网络不可达处置：保留 `.usbk` 与续传状态，记审计，**不阻塞后续介质处理** | P1 | 网络状态 | 待续传队列 | — | 静默失败不允许：必须留审计 |
| F-E09 | **出站白名单**：请求前校验目标 host 必须等于配置的 `endpoint` 主机，否则拒绝发起 | P0 | 请求目标 | 通过/拒绝 | 命中 → ERROR 并记录 | 防「配置被改指向他人服务器」 |
| F-E10 | 传输安全：仅 HTTPS；TLS 1.2+；**开启证书校验，不提供 `InsecureSkipVerify` 开关** | P0 | 连接 | 加密通道 | 证书错误 → 失败并提示 | 不接受自签降级 |
| F-E11 | 总量与速率限制：`max_total_bytes` 上限（沿用 Q-07）+ 可选 `--rate-limit` | P1 | 配置 | 截断/限速 | 超限 → 中止 + 审计 | 防单次跑飞打爆流量 |
| F-E12 | 本地保留策略：上传成功后按 `cleanup_temp`（默认 true）删除 `.usbk`；`keep_local` ≥1 时保留最近 N 份以便核对 | P1 | 作业结果 | 删除/保留 | 删除失败 → WARN，不影响作业结论 | 只删本工具自己生成的产物 |

### 1.2 凭据保护模块 `internal/cred`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-F01 | 部署文件 `client.json`：R2 凭据 + 采集策略 + 上传策略；ACL 收紧为 `SYSTEM` + `Administrators` 可读 | P0 | 生成器下发 | 受保护文件 | 权限设置失败 → 拒绝继续并明确报错 | 不做「设置失败仍继续」的静默降级 |
| F-F02 | **DPAPI 机器范围加密**（`crypt32.dll` 的 `CryptProtectData` / `CryptUnprotectData`，经 `syscall.NewLazyDLL` 调用） | P0 | 明文字段 | 密文落盘 | 解密失败（换机器/换账户）→ 明确报错 | 密文绑定本机，拷贝到别的机器解不开 |
| F-F03 | 运行时仅内存解密；使用后 `zeroize` 清零（复用 `internal/keyfile` 的缓冲区清零约定） | P0 | 密文 | 内存凭据 | — | 不清屏、不写 swap 之外的中间文件 |
| F-F04 | 最小权限：R2 API Token 侧仅授予目标 bucket + prefix 的 **Object Write**，不授予读权限、不授予 bucket 级权限 | P0 | 生成器引导 | 权限约束 | 探活发现权限过大 → WARN 提示收紧 | 权限过大 = 泄漏后损失面扩大 |
| F-F05 | 可轮换：替换 `client.json` 即完成换 token，**不需重新编译客户端** | P0 | 新凭据 | 生效 | — | 泄漏时的处置动作是「吊销 + 换文件」 |
| F-F06 | 启动自检：凭据缺失 / DPAPI 解密失败 / 字段不全 → **明确报错并禁用上传能力**，退回「仅本地产物」模式，不静默降级 | P0 | 启动流程 | 能力开关 | — | 降级必须显式且可见 |
| F-F07 | 凭据永不出现在日志、审计、命令行参数、进程命令行中（避免 `wmic process` 看到） | P0 | — | — | 检测到日志含凭据 → 视为缺陷 | 传参一律走文件读取 |
| F-F08 | **明确不接受的方案**：Secret Access Key 编译进 exe。生成器遇此请求直接拒绝并说明理由 | P0 | 生成请求 | 拒绝 | — | 编译进去 = 公开；且该二进制一旦流出，桶权限即被他人持有 |

### 1.3 采集策略模块 `internal/collectpolicy`

**原「介质准入（卷序列号白名单）」方案已废弃**（理由见 D-20）：卷序列号由 `GetVolumeInformationW`
返回，是**格式化时生成**的——重新格式化即改变；廉价 U 盘主控还常上报空白，或量产写死的重复序列号。
净结果是可用性与安全性两端同时失效：自己刚格式化过的盘被拒，量产重复号的陌生盘反而放行。

改为**显式策略三档**，默认回到原始语义（未带私钥/标记的介质一律采集，受容量上限约束）。

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-G01 | 采集策略 `collect_policy` 三档：`all`（**默认**）未带私钥/标记的介质即采集；`marker_only` 仅盘根带 `.usbbackup-r2-allow` 的介质进采集分支；`off` 关闭采集分支（只保留回写分支） | P0 | 配置 | 策略 | 取值非法 → 配置校验失败，拒绝启动 | 默认值即用户 2026-09-18 拍板的选择，不偷偷收紧 |
| F-G02 | 判定结果写入审计（`collect_policy` + 判定 + 依据），**不得成为内容外传通道** | P0 | 判定 | 审计行 | 写失败 → WARN | 不含文件名、不含内容 |
| F-G03 | `marker_only` 未命中：不打开任何文件、不打包、不上传，审计 `reason=no-marker` | P0 | 盘符 | 审计行 | — | **零读取**，只读盘根那一个文件名 |
| F-G04 | 卷序列号**降级为审计字段**（`media_serial`），不承担准入判定；读取失败记 `unknown`，不影响流程 | P0 | 盘符 | 审计字段 | 读取失败 → 记 unknown | 可变值不可作为身份依据（D-21） |
| F-G05 | 标记文件由用户显式写入（`install-usb` 已支持）；**格式化会清掉标记，需重打一次** | P1 | 命令 | 标记文件 | 写入失败 → 报错 | 标记内容是公钥指纹，不含私钥材料 |

### 1.4 客户端流程改造 `cmd/usbbackup-r2`

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-H01 | 分支判定顺序修订：卷类型 → 就绪等待 → **采集策略判定（F-G01）** → 私钥检测（F-2xx）→ 分支 A 回写 / 分支 B 打包加密上传 | P0 | 卷到达事件 | 作业结果 | 任一步失败 → 清理半成品 + 审计，不影响后续作业 | 策略前置：marker_only 未命中即不进后续任何环节 |
| F-H02 | 分支 B 流程：容量门控（F-304）→ 流式 zip（F-501）→ 混合加密（F-601）→ **R2 上传（F-E01~）** → 保留/清理（F-E12） | P0 | 盘符 | 远端对象 + 审计 | 上传失败 → F-E05/F-E08 处置 | 明文 zip 默认不落盘（沿用 D-03） |
| F-H03 | 审计日志扩展：新增 `media_serial` / `collect_policy` / `collected` / `r2_object_key` / `uploaded_bytes` / `upload_result` | P0 | 作业结果 | `audit.jsonl` | 写失败 → WARN | **不含 token、不含签名头、不含内容** |
| F-H04 | 客户端 `upload <文件.usbk>` 子命令：手动补传指定产物 | P1 | 文件路径 | 上传结果 | 文件非本工具产物 → 拒绝 | 只接受容器魔数 `USBK` 校验通过的输入 |

### 1.5 生成器扩展 `cmd/usbkeygen-r2`（接 F-906 之后）

| 编号 | 功能点 | 优先级 | 输入 | 输出 | 异常处理 | 安全注意事项 |
|---|---|---|---|---|---|---|
| F-907 | `build-client` 增补 R2 参数：交互式向导 + `--r2-config <json>`（account_id / bucket / endpoint / prefix / token / 上传策略） | P0 | 参数 | 客户端 + `client.json` | 参数缺失 → 明确列出缺哪一项 | token 不接受命令行明文传入时回显 |
| F-908 | 生成**受 DPAPI 保护的 `client.json`** 并放入部署包（不经由 exe 内嵌） | P0 | R2 配置 | 部署包 | DPAPI 调用失败 → 中止生成 | 拒绝生成不含保护层的凭据文件 |
| F-909 | `r2-check`：用配置的凭据做一次探活（`HeadBucket` + 试写一个小对象后删除），验证 endpoint / bucket / 权限是否正确且最小 | P1 | 配置 | 检查报告 | 失败 → 分项指明 DNS / TLS / 签名 / 权限 | 试写对象放在配置 prefix 下且立即删除 |
| F-910 | 生成时显著提示：token 的作用域与吊销方式、采集策略的含义与档位、凭据可被提取的残余风险、免责声明（CC BY-NC-SA 4.0） | P0 | 无 | 控制台输出 | — | 三段提示不得折叠进滚动日志 |

---

## 2. R2 配置项

| 字段 | 必填 | 默认 | 说明 |
|---|---|---|---|
| `account_id` | 是 | — | Cloudflare 账户 ID |
| `bucket` | 是 | — | 目标存储桶 |
| `endpoint` | 是 | `https://<account_id>.r2.cloudflarestorage.com` | 出站白名单唯一允许的目标主机（F-E09） |
| `prefix` | 否 | `usb/<序列号>/` | 对象键前缀 |
| `object_key_template` | 否 | `{prefix}{时间戳}_{卷标}.zip.usbk` | 卷标先经 F-802 净化 |
| `access_key_id` | 是 | — | R2 API Token 的 Access Key ID |
| `secret_access_key` | 是 | — | R2 API Token 的 Secret；**只存在于 DPAPI 加密文件中** |
| `region` | 否 | `auto` | R2 固定 `auto` |
| `multipart_threshold_bytes` | 否 | `5 GiB` | 超过则走分片（F-E03） |
| `part_size_bytes` | 否 | `64 MiB` | 分片大小，下限 5 MiB |
| `max_retries` | 否 | `5` | 重试次数（F-E05） |
| `upload_timeout_min` | 否 | `30` | 单次上传总超时 |
| `max_total_bytes` | 否 | `10 GiB` | 单次上传体积上限（沿用 Q-07 口径：比的是待打包数据量） |
| `cleanup_temp` | 否 | `true` | 上传成功后是否删除本地 `.usbk` |
| `keep_local` | 否 | `0` | `>0` 时保留最近 N 份本地产物 |
| `collect_policy` | 否 | `all` | 采集策略档位：`all` / `marker_only` / `off`（F-G01） |

---

## 3. 凭据保护方案（替换原「garble + 硬编码」方案）

原方案要求把 Access Key ID / Secret Access Key 硬编码进 exe 并做高级混淆。**本方案不采用**，理由与替代如下。

| 维度 | 原始需求方案（硬编码 + garble） | 本项目方案 |
|---|---|---|
| 存储位置 | 编译进 exe 的数据段 | 独立的 `client.json`，DPAPI 机器范围加密 |
| 被提取后果 | 任何拿到 exe 的人都能提取，持有桶的读写权限 | 密文绑定本机账户，拷走文件无法解开 |
| 权限范围 | 通常沿用完整 token 权限 | 仅目标 bucket + prefix 的 Object Write，无读权限（F-F04） |
| 泄漏处置 | 必须重新编译并重新分发所有客户端 | 替换 `client.json` 或吊销 token，**不需重编译**（F-F05） |
| 混淆的作用 | 被当作保护手段，但只提高逆向成本，不改变"可提取"这一事实 | 不引入 garble：它破坏 N-104「零依赖 + 可离线构建」，且保护不了 exe 之外的凭据文件 |
| 残余风险 | 高且不可控 | 有界：本机管理员/SYSTEM 可读；已在此前提下设计权限最小化与轮换 |

**必须向用户明示**（写入 README / DISCLAIMER / 生成器提示 F-910）：任何落在目标机器上的凭据都存在被提取的可能，尤其是具备本机管理员权限的主体。因此本项目的防护目标是**把损失面压到最小并让轮换成本接近零**，而不是声称"不可提取"。

---

## 4. 异常处理与重试

| 场景 | 处置 | 用户可见反馈 |
|---|---|---|
| 缺 `client.json` / DPAPI 解密失败 | 禁用上传，退回仅本地产物模式（F-F06） | ERROR + 明确原因与修复指引 |
| DNS 解析失败 / 无网络 | 保留产物 + 续传状态，记审计，不阻塞后续介质（F-E08） | WARN |
| TLS 证书错误 | 直接失败，不降级（F-E10） | ERROR |
| SigV4 签名被拒（403） | 不重试，指向时钟偏移 / token 权限两项排查 | ERROR + 排查清单 |
| 分片上传中断 | 重试该片；超次数则 `AbortMultipartUpload` | WARN + 审计 |
| `uploadID` 失效 | 放弃续传，从头开始 | INFO |
| 上传后大小不一致 | 重传一次；仍不一致则标记失败并保留本地 | ERROR |
| 介质中途拔出 | 中止作业，清理半成品，不损伤源盘 | WARN |
| `collect_policy=off` | 采集分支整体关闭（F-G01） | INFO + 说明只走回写分支 |
| 磁盘空间不足 | 中止 + 清理半成品 | ERROR |

---

## 5. 安全边界修订

| 编号 | 约束 |
|---|---|
| **G-06′（修订）** | **单向最小出站**：网络能力从「零网络」修订为「仅到配置的 R2 endpoint 主机的 HTTPS 出站」，用途仅限上传本工具产出的加密容器。**不得引入任何入站监听、不接受远程指令、不存在控制通道（C2）、不做心跳上报内容**。除该 endpoint 外不得有其它出站调用（F-E09 强制校验）。 |
| G-12（修订） | **采集策略显式化**：采集分支由配置项 `collect_policy` 决定（`all` / `marker_only` / `off`），默认 `all`。**不再使用卷序列号白名单** —— 序列号在格式化后改变、廉价盘可能重复，作为身份依据不可靠（D-20）。策略是执行手段，G-15 的使用前提不因此放宽。 |
| G-13 | **凭据不进二进制**：R2 凭据以 DPAPI 加密文件在部署时下发，权限最小化、可独立轮换；不接受 Secret Access Key 编译进 exe。 |
| G-14 | **出站面收敛**：唯一允许的出站目标是配置的 R2 endpoint；仅 HTTPS 且强制证书校验。 |
| G-15 | **使用边界收紧**：客户端仅部署于本人自有或本人管理的 Windows 机器；仅处理本人自有的可移动介质。**前提不成立时本项目不适用**。 |
| G-01~G-05、G-07~G-11 | 沿用基础需求表，不修改。 |

---

## 6. 关键设计决策（接 D-12 之后）

| 编号 | 决策 | 理由 | 备选与否决原因 |
|---|---|---|---|
| D-13 | 用**标准库手写 S3 SigV4** 与 R2 交互，不引入 AWS SDK | 保住 N-104「零第三方依赖 + 可离线构建」；R2 兼容 S3 API 且签名算法固定，实现面约 200 行 | AWS SDK for Go v2：数百个传递依赖，破坏离线构建；`minio-go`：同样破依赖 |
| D-14 | 凭据用 **DPAPI 机器范围加密 + 独立文件**，不编译进 exe | 编译进去即为公开；DPAPI 绑定本机可挡住"拷文件到别处解密"；换 token 不需重编译 | 内嵌 + garble：只提高成本，不改变可提取事实；纯 ACL：文件被拷走即失效 |
| D-20 | **废弃卷序列号白名单准入**（用户 2026-09-18 判断"大概率行不通"，成立） | 序列号是格式化时生成的：重装/重格式化后自己合法的盘会被拒（可用性失效）；廉价 U 盘主控常上报空白或量产复制的重复号，陌生盘反而可能被放行（安全性失效）——两头都保不住。改为 `collect_policy` 三档显式策略，默认回到「未标记介质一律采集」 | 保留白名单：拿一个会变的值当身份，等于用可用性换虚假的安全感；改用硬件序列号需 SetupAPI 打开物理设备，成本高且廉价盘仍可能重复 |
| D-21 | 卷序列号**降级为审计字段** | 它擅长的场合是事后对账（"这次采的是哪块盘"），不是事前唯一识别 | 完全不记录：丢掉事后追溯能力 |
| D-16 | 上传走分片（阈值 5 GiB / 分片 64 MiB） | S3 单次 PUT 上限 5 GiB；本项目门控上限 10 GiB，必然超过 | 单次 PUT：10 GiB 场景直接失败 |
| D-17 | 修订而非删除 G-06 | 原红线的实质是「收敛网络面」，不是「禁止一切网络」。修订后仍可被 `strings` 复核出唯一目标主机 | 直接删除 G-06：丢掉可验证的收敛约束 |
| D-18 | 不引入 garble | 破坏零依赖与离线构建；且它保护的是代码，而凭据在代码之外 | garble + 硬编码：见 D-14 |
| D-19 | 上传失败不阻塞后续介质处理 | 常驻服务被一次网络故障卡死会导致后续所有介质漏处理 | 阻塞重试到成功：网络长期不可用即整体失效 |

---

## 7. 待确认项

| 编号 | 待确认 | 当前默认 | 影响 |
|---|---|---|---|
| Q-09 | 上传成功后本地 `.usbk` 是否保留 | `cleanup_temp=true` 即删；`keep_local=0` | 磁盘占用 vs 事后核对能力 |
| Q-10 | 对象键模板 | `usb/<序列号>/<时间戳>_<卷标>.zip.usbk` | 远端目录结构 |
| Q-11 | 是否需要断点续传 | 需要（P1） | 大对象失败重传成本 |
| Q-12 | 多介质并发上传 | 串行（沿用 F-104 作业队列） | 多盘同时插入时的耗时 |
| Q-13 | 是否限速 | 默认不限，配置可开 | 网络占用 |
| Q-14 | 远端对象生命周期 | 建议在 R2 bucket 侧配 lifecycle rule | 远端存储成本 |
| Q-15 | 采集策略默认档位 | `all`（用户 2026-09-18 拍板） | 收紧到 `marker_only` 需给每块盘打标记，格式化后要重来 |

---

## 8. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M6 | `internal/cred`（DPAPI）+ `internal/r2`（SigV4 + 分片 + 重试）+ 单测（本地假 R2 用 `net/http/httptest`） | ⬜ 待开始 |
| M7 | `internal/collectpolicy` 采集策略 + 客户端流程改造（F-H01~F-H04）+ 生成器扩展（F-907~F-910） | ⬜ 待开始 |
| M8 | Windows 实机验证（真实 R2 桶）+ 文档更新（README / DISCLAIMER / TUTORIAL）+ 规范提交 | ⬜ 待开始 |

### 8.1 已发现的既有文档缺陷（本次一并修）

| # | 缺陷 | 处理 |
|---|---|---|
| 1 | `REQUIREMENTS.md` 中 `F-C01`~`F-C09` 编号**重复占用**：§1.12（日志与配置）用 F-C01~F-C05，§5.1（部署形态）用 F-C01~F-C09 | ✅ 已改：§5.1 的编号改为 `F-K01`~`F-K09` |
| 2 | G-06「零网络」的表述散落在 `README.md`、`DISCLAIMER.md`、`internal/cli/banner.go`（**用户可见的警告正文**）、`internal/copier/copier.go` | ✅ 已修订为 G-06′ 并同步全部四处；`banner_test.go` 增加**反向守卫**（出现"不进行任何网络/零网络/无网络通信"即测试失败），防止旧表述回归 |

> 第 2 条是必须做的：横幅是 F-901 要求"显著展示"的安全警告，加了出站能力之后
> 还写着"不进行任何网络通信"，就不只是文档过期，而是**对用户的虚假陈述**。
