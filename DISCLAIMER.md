# 免责声明 / DISCLAIMER

**项目**：`usbbackup-r2` / `UsbBackUP-R2` —— Windows 专用 USB 自动备份与 R2 加密上传工具
**文档版本**：v1.0 · 2026-09-18
**许可协议**：CC BY-NC-SA 4.0（详见 `LICENSE`）

---

## 一、安全警告

1. **本工具会对接入本机的可移动存储介质执行自动读取与打包。** 在运行前，你必须完整阅读本声明与 `README.md`，并确认自己理解其行为。
2. **本工具会对**通过准入判定的**介质，把其**全部内容**打包为加密容器。** 该容器先落在本机产物目录，随后**上传至你在配置中指定的 Cloudflare R2 存储桶**。请确认你对所接入介质内的全部数据拥有合法处置权，并已自行评估把该数据存放在第三方云服务上的合规影响。
3. **本工具不包含任何"把本机数据回写进介质"的分支。** 对源介质**全程只读**：不写入、不删除、不改名、不移动源盘上的任何文件。唯一的写动作落在本机产物目录与远端对象存储上。
4. **密钥与口令一旦丢失，加密容器将无法恢复。** 本工具不提供任何后门、密钥托管或找回机制。请自行备份私钥与口令。
5. **本工具不含任何加壳、免杀、反调试、隐藏进程或绕过安全软件的能力。** 若你的安全软件对其告警，请自行判断后再决定是否放行。
6. **本工具具备且仅具备一项网络能力：** 向你在配置中指定的 R2 端点（**唯一允许的目标主机**）通过 HTTPS 执行 S3 的写入、读取、列举与删除。它**没有入站监听、不接受任何远程指令、不存在控制通道**，也不做遥测或联网校验；除该端点外不会访问任何地址。目标主机在代码层校验，不接受配置指向其他服务器（仅回环地址允许走明文，供自带假服务端联调）。
7. **上传凭据存放在受保护的文件中，共有三种保护方式，强度不同。** 默认是 Windows DPAPI **机器范围**加密（以该机器身份即可解开；本机管理员/SYSTEM 可以提取）；也可用口令加密（PBKDF2-SHA256 + AES-256-GCM，跨平台可读，强度取决于口令与口令文件的保管）；还可用**明文**（仅靠 0600 文件权限，**等于没有加密**，仅供既无 DPAPI 又无法在启动时提供口令的极端场景）。凭据应授予**可实现的最小权限**并定期轮换。请注意 R2 的权限档位现实：控制台可签发的长效 token 中，`Object Read & Write`（桶级，可限定到桶但**不能限定到 key 前缀**）即是可达的最小档位，它**同时具备读取、覆盖与删除对象的能力**。R2 **不提供**对象版本控制（那套 S3 接口它没有实现），因此能阻止覆盖或删除的机制只剩**桶锁（Bucket lock rules）**——**本项目明确不启用它**（2026-09-20 决定），是否启用及由此产生的删除风险由你自行评估与承担。**任何落在机器上的凭据都存在被本机高权限主体提取的可能，本项目不声称其不可提取** —— 防护目标是压缩损失面、让轮换成本接近零，而非保证不可提取。因凭据泄漏、口令文件与凭据放在同一处、权限配置过宽、或因远端对象被删除而导致的任何后果由你自行承担。
8. **授予远端读取权限即意味着密文可被他人下载。** 若你把存储桶接成公开域名，则任何知道对象键的人都能获取密文。容器内容是端到端加密的（没有私钥读不出明文），但**对象键中的卷标与时间戳等元数据会暴露**。是否公开、公开到何种程度，由你自行决定并承担后果。

---

## 二、使用授权边界

**本工具的合法使用范围仅限于：**

- 你**个人自有、或你有完全管理权**的计算机设备；
- 你**个人自有**的可移动存储介质。

介质侧的采集范围由配置项 `collect_policy` 控制：默认档位（`all`）会采集任何**未命中授权标记**的介质（仍受容量上限约束），另有 `marker_only`（仅采集带采集标记的介质）与 `off`（关闭采集）两档可供收紧。**工具不再用硬件身份（卷序列号）判断权属** —— 该值在格式化后即改变，而廉价 U 盘主控还可能上报空白或重复序列号，两个方向都不可靠。因此最终判断权属的责任在你，不在工具。

两个标记的语义：

| 标记 | 位置 | 含义 |
|---|---|---|
| `.usbbackup-allow` | **盘根** | "这块盘是我自己的，别动它" → **豁免**，任何策略档位下都不采集 |
| `.usbbackup-collect` | **盘根** | "这块盘的内容可以打包上传" → 仅 `marker_only` 档位下作为放行条件 |

**豁免判定优先于一切策略档位**，不受 `collect_policy` 影响。

**明确禁止的使用方式包括但不限于：**

- 将本工具部署于不属于你、或不由你管理的设备；
- 处理你无权处置的存储介质内的数据，包括他人介质与来路不明的介质；
- 以获取他人隐私、商业秘密、账号凭据或任何非授权数据为目的使用本工具；
- 规避、对抗任何组织的数据安全策略、审计机制或合规要求。

**你须自行确保使用行为符合所在国家/地区的法律法规**，包括但不限于《中华人民共和国网络安全法》《中华人民共和国数据安全法》《中华人民共和国个人信息保护法》以及所在司法辖区的相关法律。因使用本工具而产生的任何法律后果，由使用者自行承担。

---

## 三、免责条款

1. **本软件按"现状"（AS IS）提供**，不附带任何形式的明示或默示担保，包括但不限于对**适销性、特定用途适用性、不侵权、无错误、不中断**的担保。
2. **作者与贡献者不对任何直接、间接、附带、特殊、惩罚性或后果性损害承担责任**，包括但不限于：数据丢失、数据损坏、数据泄露、业务中断、设备损坏、利润损失、商誉损失，无论其如何引起、基于何种责任理论，亦无论是否已被告知此类损害的可能性。
3. **作者与贡献者不提供任何形式的技术支持、可用性保证或维护承诺。** 软件可能包含缺陷，使用风险完全由你自行承担。
4. **加密强度依赖使用者自身操作**：包括是否使用足够强度的密钥、是否妥善保管私钥与口令、是否在可信环境中运行。作者不对因密钥管理不当导致的任何后果负责。
5. **使用者须自行完成测试与评估。** 在将本工具用于任何重要数据之前，你必须在**非生产环境**中先行验证其行为符合预期。
6. **本工具不对存储介质内原有数据做删除或改写**（详见 `README.md` 的行为说明）。但**使用者仍须自行备份重要数据**，作者不对任何数据状态的变化负责。
7. **上传行为与第三方服务。** 本工具会把加密容器上传至你自行配置的 Cloudflare R2 账户。你须自行确保该账户的配置安全、凭据权限最小化与使用合规。因凭据泄漏、桶配置不当、远端生命周期策略缺失、或将你无权处置的数据上传至该账户而产生的任何后果，由你自行承担。
8. 若你所在司法管辖区的法律不允许上述部分免责条款，则该部分条款在最小必要范围内不予适用，其余条款继续有效。

---

## 四、许可与再分发

本项目采用 **Creative Commons Attribution-NonCommercial-ShareAlike 4.0 International（CC BY-NC-SA 4.0）** 协议发布。完整法律文本见 `LICENSE` 或
<https://creativecommons.org/licenses/by-nc-sa/4.0/legalcode>。

| 权利 | 说明 |
|---|---|
| **署名（BY）** | 再分发或改编时必须保留原作者署名与本声明 |
| **非商业性使用（NC）** | **不得将本项目或其改编作品用于任何商业用途** |
| **相同方式共享（SA）** | 改编作品必须以相同协议（CC BY-NC-SA 4.0）分发 |
| 无附加限制 | 不得施加法律条款或技术措施限制他人获得本协议授予的权利 |

在遵守上述条件的前提下，你可以自由地**复制、分发、修改**本作品。

---

## 五、确认

继续使用本工具，即表示你**已阅读、理解并同意**上述全部条款。若你不同意其中任何一条，请立即停止使用并删除本软件及其全部副本。
